// title (a string), body (a string), rating (an integer), and date (a DateTime).

class JournalEntry {
  int id;
  String title;
  String body;
  int rating;
  DateTime date;

  JournalEntry (this.id, this.title, this.body, this.rating, this.date);
}